#include <stdio.h>
#define PI 3.14159
uint8_t = 255;

int main(void)
{
    float radius,area;
    //Calculate area of circle
    radius = 12;
    area = PI * radius*radius;
    printf("Area = %f\n",area);
}